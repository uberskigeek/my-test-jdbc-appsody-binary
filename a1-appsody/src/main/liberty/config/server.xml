<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<server description="copied from Liberty server generated by Boost">
    <featureManager>
        <feature>cdi-2.0</feature>
        <feature>mpHealth-1.0</feature>
        <feature>jdbc-4.2</feature>
        <feature>jaxrs-2.1</feature>
        <feature>mpRestClient-1.1</feature>

        <!-- For connection validation -->
        <feature>appSecurity-2.0</feature>
        <feature>restConnector-2.0</feature>

    </featureManager>

    <httpEndpoint host="*" httpPort="9080" httpsPort="9443" id="defaultHttpEndpoint"/>

    <quickStartSecurity userName="apps" userPassword="ody"/>

    <application context-root="/" location="a1-war-1.0.war" type="war"/>

    <library id="Library1">
        <fileset dir="${shared.resource.dir}" includes="mysql-connector-java-8.0.15.jar"/>
    </library>

    <!-- WORKAROUND -->

    <dataSource id="DefaultDataSource" jdbcDriverRef="JdbcDriver1">
        <properties databaseName="${boost_db_databaseName}" password="${boost_db_password}" portNumber="${boost_db_portNumber}" serverName="${boost_db_serverName}" user="${boost_db_user}"/>
    </dataSource>
    <jdbcDriver id="JdbcDriver1" libraryRef="Library1"/>
</server>
